package main_test

import (
	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

var _ = Describe("main", func() {
	It("loads moves.yml into memory", func() {
		Expect(true).To(BeTrue())
	})
})
